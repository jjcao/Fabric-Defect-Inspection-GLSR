
%%%%%%% 返回值的意义：c是不等式约束条件。即c<=0;
%%%%%%%              f是等式约束条件。即c=0；
%%%%%%%              f（m）就是文章中所说的：c0^2+c1^2+c2^2+c3^2=0;和c2*c0+c3*c1=0;
%优化低通滤波系数，而约束条件针对高通滤波
function [c,f] = fconwav(x,n)  %约束条件
        c =[];
        N = n;
%     for j=1:n
%          g(j)=(-1)^(n-j)*x(n-j+1);
%     end
    
    for m = 1:N/2
        f(m) = sumk(x,m,N) - deta(m-1);%f = x(k)*x(k+2*(m-1)),k;
     end
   
   